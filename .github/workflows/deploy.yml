name: Deploy to CloudFront

on:
  push:
    branches:
      - main
    paths:
      - 'website/**'
      - 'edge-function/**'
      - '.github/workflows/**'
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  S3_BUCKET: jesusrugama.demos.edge-geolocation
  CLOUDFRONT_DISTRIBUTION_ID: E2STMHSPBIMH7V

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::464624688884:role/jesusrugama-sites-github-actions-role
          aws-region: ${{ env.AWS_REGION }}

      # ---- Website ----
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: website/package-lock.json

      - name: Install website dependencies
        working-directory: website
        run: npm ci

      - name: Build website
        working-directory: website
        run: npm run build

      - name: Deploy website to S3
        run: |
          aws s3 sync website/dist s3://${{ env.S3_BUCKET }}/website --delete

      # ---- Edge Function ----
      - name: Build edge function
        working-directory: edge-function
        run: |
          npm ci
          npm run package

      - name: Upload edge function to S3
        run: |
          aws s3 cp edge-function/function.zip s3://${{ env.S3_BUCKET }}/lambda/edge-geolocation.zip

      - name: Update Lambda function code
        run: |
          aws lambda update-function-code \
            --function-name edge-geolocation \
            --s3-bucket ${{ env.S3_BUCKET }} \
            --s3-key lambda/edge-geolocation.zip \
            --region ${{ env.AWS_REGION }}

      # ---- CloudFront ----
      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ env.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"
